version: "3.5"
services:
        server:
                image: test-forwarder
                build: .
                environment:
                        - "MODE=server"
                ports:
                        - "2222:22"
        client:
                image: test-forwarder
                build: .
                volumes:
                        - ./tunnels.conf:/root/tunnels.conf
                environment:
                        - "MODE=client"
                        - "CONF=tunnels.conf"

